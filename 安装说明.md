# PaperMate 一键安装说明

> English version: [INSTALL.md](INSTALL.md) · 项目介绍：[README.zh-CN.md](README.zh-CN.md)

这套脚本会把论文助手打包成桌面上可直接启动的本地应用，不需要手动敲命令，也不需要管理员权限。

## 一键安装

1. 双击项目根目录里的 `一键安装.bat`。
2. 如果电脑上还没有安装 PaperMate，安装器会先弹出“选择 PaperMate 安装位置”窗口。默认推荐 `D:\PaperMate`，也可以点“新建文件夹”改成任意位置，然后点“选择文件夹”。如果直接选择了磁盘根目录（如 `D:\`），安装器会自动安装到 `D:\PaperMate`。
3. 如果检测到 `%LOCALAPPDATA%\PaperMate\config.json` 里的已安装版本，安装器不会再弹目录选择窗口，而是直接升级原安装位置。
4. 等待自动安装依赖、构建正式版本并创建快捷方式，完成后直接关闭窗口即可。

安装器会把项目文件复制到所选位置，再在复制后的项目里安装依赖和构建。原项目文件夹不会被改动；升级时安装位置已有的 `data` 数据文件夹会原样保留。

安装完成后，桌面会出现：

- `PaperMate 论文助手`：双击启动本地服务并打开阅读界面。

开始菜单的 `PaperMate` 文件夹里还包含启动、停止服务和卸载三个入口。启动快捷方式使用 PaperMate 专属图标，卸载入口使用独立的卸载图标。安装目录里还有 `一键卸载.bat`，旁边放着带卸载图标的 `卸载 PaperMate 论文助手` 快捷方式。

## 覆盖安装与升级

安装与更新都由 `一键安装.bat` 完成，不需要单独的升级脚本：

- 首次安装：弹出“选择 PaperMate 安装位置”窗口，安装到所选位置。
- 已安装过：自动读取 `%LOCALAPPDATA%\PaperMate\config.json`，跳过目录选择，直接覆盖更新原安装位置。
- 升级流程：先停止正在运行的 PaperMate 服务，用最新源码覆盖安装目录，重新安装依赖并构建正式版本，再刷新启动器、快捷方式和 Windows 应用列表版本号。
- 安装位置的 `data` 文件夹会完整保留，包括 `papermate.db`、WAL/SHM 文件和 `papermate-backup.json`。

## 日常使用

双击桌面上的 `PaperMate 论文助手` 即可。服务会在后台启动，然后自动用 Edge 或 Chrome 打开 `http://127.0.0.1:3000`。

- 再次双击图标时，如果服务已经在运行，会直接打开页面，不会重复启动。
- 端口 3000 被占用时，会自动改用其他空闲端口。
- 想在不用时停掉后台服务，可以在开始菜单的 `PaperMate` 文件夹里点击 `停止 PaperMate 服务`。

## 一键卸载

双击安装目录里的 `一键卸载.bat`，或旁边的 `卸载 PaperMate 论文助手` 快捷方式，也可以从开始菜单的 `PaperMate` 文件夹点击 `卸载 PaperMate 论文助手`。

卸载会：

- 停止正在运行的 PaperMate 服务。
- 删除桌面启动快捷方式（如存在）、开始菜单快捷方式，以及安装目录中的卸载快捷方式。
- 删除 `%LOCALAPPDATA%\PaperMate` 下的启动器与日志。
- 从 Windows 的“已安装的应用”列表移除 PaperMate。
- 删除安装位置中的应用文件、依赖和构建产物。

论文、笔记和本机备份会保留在安装位置的 `data` 文件夹中，不会随卸载丢失。如果卸载时想连数据一起删除，需要单独运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$env:LOCALAPPDATA\PaperMate\uninstall.ps1" -RemoveAllData
```

如果当时选择的是“安装到原项目目录”，卸载不会删除原项目源码，只会移除快捷方式、启动器和应用列表项。

## 环境要求

- Windows 10 或 Windows 11。
- Node.js 22.5 或更高版本。如果电脑没有安装或版本过低，安装脚本会自动尝试通过 winget 安装或升级到 Node.js LTS。
- 首次安装需要联网下载 npm 依赖。
- 安装位置需要是一个普通文件夹，不能是项目文件夹的上级目录，也不能选在项目文件夹内部。直接选磁盘根目录时，安装器会自动使用根目录下的 `PaperMate` 文件夹。
- 论文数据保存在安装位置的 `data/papermate.db`（SQLite）；完整 JSON 只在执行“立即备份”或导出时写入 `data/papermate-backup.json`。

## 从 Windows 应用列表卸载

安装后也可以打开“设置 → 应用 → 已安装的应用”，找到 `PaperMate 论文助手` 后点击卸载。
