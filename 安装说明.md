# PaperMate 安装说明

> English version: [INSTALL.md](INSTALL.md) · 项目介绍：[README.zh-CN.md](README.zh-CN.md)

PaperMate 是本地运行的应用（默认 `http://127.0.0.1:3000`）。有两种安装方式，装好之后由**应用内自动更新**负责日常更新。

| 安装方式 | 适合谁 | 需要 Node.js | 大致耗时 |
| --- | --- | --- | --- |
| **预编译包安装** | 只想用的人（推荐） | 不需要，包内自带 `node.exe` | 1 分钟内 |
| **源码安装** | 要改代码的开发/贡献者 | 需要 Node.js 22.5+ | 首次 3–10 分钟 |

两种方式用的是**同一个入口** `一键安装.bat`：它先看当前目录是什么——里面有 `server.js` / `node.exe`（从 Release 的 zip 解压出来的）就走上面的预编译包安装；是完整源码（有 `app\`、`components\`、`lib\`、`next.config.ts`）就走下面的源码安装。所以不需要记两套命令。

## 方式一：预编译包安装（推荐普通用户）

1. 打开项目的 GitHub **Releases** 页面，下载最新的 `papermate-windows-x64.zip`。
2. 解压到任意目录（**不要**放在 `C:\Program Files` 这类需要管理员权限的位置）。
3. 双击解压目录里的 `一键安装.bat`。
4. 首次安装会弹出“选择 PaperMate 安装位置”窗口，默认推荐 `D:\PaperMate`；确认后几十秒装完。
5. 双击桌面上的 `PaperMate 论文助手` 启动。

预编译包安装的特点：

- 不需要 Node.js、npm，也不需要联网——包内自带 `node.exe`。
- 覆盖安装时先把整个旧目录挪成同级的 `.papermate-backup-<guid>`，再把用户数据还原回来：`data\` 整个目录，以及你改过的 `public\*.txt`（提示词、拾句、人格）。想回滚就先停服务，把备份目录改回原安装路径。
- 也可以把 zip 直接**拖到** `一键安装.bat` 上安装。
- 命令行等价写法：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\install-package.ps1 -PackageZip D:\下载\papermate-windows-x64.zip -InstallDir D:\PaperMate -Start
```

- 如果目标目录不是空的、又不像是 PaperMate 安装目录，安装器会拒绝覆盖，需要显式加 `-Force`。
- 安装后会创建桌面与开始菜单快捷方式，并注册到 Windows“已安装的应用”列表。
- 解压出来的包里还有 `一键卸载.bat`，装好后安装目录里也有一份；卸载也可以从开始菜单的 `PaperMate` 文件夹或 Windows“已安装的应用”进入。

## 方式二：源码安装

### 一键安装

1. 双击项目根目录里的 `一键安装.bat`。
2. 如果电脑上还没有安装 PaperMate，安装器会先弹出“选择 PaperMate 安装位置”窗口。默认推荐 `D:\PaperMate`，也可以点“新建文件夹”改成任意位置，然后点“选择文件夹”。如果直接选择了磁盘根目录（如 `D:\`），安装器会自动安装到 `D:\PaperMate`。
3. 如果检测到 `%LOCALAPPDATA%\PaperMate\config.json` 里的已安装版本，安装器不会再弹目录选择窗口，而是直接升级原安装位置。
4. 等待自动安装依赖、构建正式版本并创建快捷方式，完成后直接关闭窗口即可。

安装器会把项目文件复制到所选位置，再在复制后的项目里安装依赖和构建。原项目文件夹不会被改动；升级时安装位置已有的 `data` 数据文件夹会原样保留。

安装完成后，桌面会出现：

- `PaperMate 论文助手`：双击启动本地服务并打开阅读界面。

开始菜单的 `PaperMate` 文件夹里还包含启动、停止服务和卸载三个入口。启动快捷方式使用 PaperMate 专属图标，卸载入口使用独立的卸载图标。安装目录里还有 `一键卸载.bat`，旁边放着带卸载图标的 `卸载 PaperMate 论文助手` 快捷方式。

### 一键升级

双击项目根目录里的 `一键升级.bat`（或再次双击 `一键安装.bat`），即可用当前项目源码升级已经安装的 PaperMate：

- 自动读取 `%LOCALAPPDATA%\PaperMate\config.json`，找到安装位置、端口和快捷方式设置。
- 升级前先停止正在运行的 PaperMate 服务。
- **增量更新，只更新需要的部分**：
  - 安装器先对源码做指纹比对（文件名、大小与修改时间），源码没有变化时直接跳过项目复制、依赖安装与构建，几秒完成；
  - 源码有变化时，只复制有变化的文件，不重复拷贝整个项目；
  - `package.json` / `package-lock.json` 没有变化时跳过 `npm install`，复用已有的 `node_modules`；
  - 只有真正需要时才重新构建正式版本。
- 每次升级都会刷新启动器、快捷方式和 Windows 应用列表版本号；安装位置的 `data` 文件夹始终完整保留，包括 `papermate.db`、WAL/SHM 文件和 `papermate-backup.json`。
- 需要强制全量重建时，可手动运行：`powershell -File scripts\install.ps1 -Upgrade -ForceFull`。

> 注意：`一键升级.bat` 会先判断本机安装形态。如果安装目录是**预编译包安装**（用 `一键安装.bat` 装的，或曾经被应用内自动更新升级过），它会改走“把当前源码构建好、只覆盖运行时产物”的部署方式（见下一节），前提是当前目录是一份**完整的开发版源码**。如果你手上只有从 Release 下载并解压出来的包、没有源码，请改用应用内“检查更新”，或重新下载 Release 的预编译包。

### 日常使用

双击桌面上的 `PaperMate 论文助手` 即可。服务会在后台启动，然后自动用 Edge 或 Chrome 打开 `http://127.0.0.1:3000`。

- 再次双击图标时，如果服务已经在运行，会直接打开页面，不会重复启动。
- 端口 3000 被占用时，会自动改用其他空闲端口。
- 想在不用时停掉后台服务，可以在开始菜单的 `PaperMate` 文件夹里点击 `停止 PaperMate 服务`。

### 一键卸载

双击安装目录里的 `一键卸载.bat`，或旁边的 `卸载 PaperMate 论文助手` 快捷方式，也可以从开始菜单的 `PaperMate` 文件夹点击 `卸载 PaperMate 论文助手`。

卸载会：

- 停止正在运行的 PaperMate 服务。
- 删除桌面启动快捷方式（如存在）、开始菜单快捷方式，以及安装目录中的卸载快捷方式。
- 删除 `%LOCALAPPDATA%\PaperMate` 下的启动器与日志。
- 从 Windows 的“已安装的应用”列表移除 PaperMate。
- 删除安装位置中的应用文件、依赖和构建产物。

论文、笔记和本机备份会保留在安装位置的 `data` 文件夹中，不会随卸载丢失。如果卸载时想连数据一起删除，需要单独运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$env:LOCALAPPDATA\PaperMate\uninstall.ps1" -RemoveAllData
```

如果当时选择的是“安装到原项目目录”，卸载不会删除原项目源码，只会移除快捷方式、启动器和应用列表项。

## 开发与测试：一键部署开发版到本地运行

开发版的版本号**固定为 1**：改代码不用动版本号，部署出去在应用里也显示版本 1。正式版本号只在纯净上传仓库（`papermate github`）里维护——发版时在那里把版本号改成真实版本（如 4.1.1）再打 tag。

完整发版链路（构建 → 打包 8000+ 个文件 → 压缩 → SHA-256 → 更新助手换目录）要十几分钟，验证一个改动不值得走这条路。真正决定页面行为的只有三样：`.next`（约 8 MB）、`server.js`、`package.json`。

- 改完代码 → 双击工作区根目录的 `一键升级.bat`，脚本按本地安装的形态自动选路：
  - **预编译包安装**（安装目录里有 `server.js` / `node.exe`，即 Release 包或应用内更新装出来的）：跑测试 → 构建 → 备份当前运行时 → 只覆盖 `.next`、`server.js`、`package.json`、5 个运行时脚本（apply-update、launch-update、start-papermate、stop-papermate、uninstall） → 重启服务 → 回读版本号 → 打开页面。通常 1 分钟内完成。
  - **源码安装**（`install.ps1 -Upgrade` 装的）：改走源码增量升级，完成后启动服务并打开页面。
- 手上有一个 `.patch` 文件 → 直接拖到 `一键升级.bat` 上（等价于 `-Patch xxx.patch`）。脚本会先 dry-run 校验补丁，再打上、跑测试、构建；补丁留下 `.rej` / `.orig` 残留会立即中止，不会动安装目录。
- 测完不满意 → 回滚最近一次开发版部署：

```powershell
powershell -File scripts\upgrade-local.ps1 -Undo
```

- 查看有哪些可回滚的备份：`powershell -File scripts\upgrade-local.ps1 -ListBackups`。备份在 `%LOCALAPPDATA%\PaperMate\dev-backups\`，默认保留最近 5 份。
- 引擎是 `scripts\quick-patch.ps1`（`一键升级.bat` 调它），常用开关：`-SkipTests`、`-SkipBuild`、`-NoRestart`、`-NoBrowser`、`-IncludePublic`（把 `public\` 一起覆盖；默认跳过 `prompts.txt`、`quotes.txt`、`buddy-personas.txt` 这三个用户可改的文件，加 `-ForcePublic` 才连它们一起覆盖）。

注意事项：

- “覆盖运行时产物”那套只对**预编译包安装**有效；源码安装由 `一键升级.bat` 自动改走 `install.ps1 -Upgrade`。
- 依赖清单（`package.json` 的 dependencies）有变化时会自动补拷 standalone 追踪到的 `node_modules`，避免覆盖完启动即 MODULE_NOT_FOUND。
- 部署出去的是开发构建：`papermate-version.json` 会标 `devBuild: true`，应用里显示版本 `1`，因此**应用内更新会一直提示“有新版本 4.x”**——这是正常现象，点它会把这台机器恢复成正式包。
- 想立刻用正式包恢复：

```powershell
powershell -File scripts\install-package.ps1 -PackageZip <官方 zip> -Force
```

- 更快的迭代方式（完全不构建、不重启）：在源码目录跑 `npm run dev`，改完热更新，浏览器刷新即可。

## 环境要求

- Windows 10 或 Windows 11。
- **预编译包安装**：什么都不用装。
- **源码安装**：Node.js 22.5 或更高版本。如果电脑没有安装或版本过低，安装脚本会自动尝试通过 winget 安装或升级到 Node.js LTS；首次安装需要联网下载 npm 依赖。
- 安装位置需要是一个普通文件夹，不能是项目文件夹的上级目录，也不能选在项目文件夹内部。直接选磁盘根目录时，安装器会自动使用根目录下的 `PaperMate` 文件夹。
- 论文数据保存在安装位置的 `data/papermate.db`（SQLite）；完整 JSON 只在执行“立即备份”或导出时写入 `data/papermate-backup.json`。

## 从 Windows 应用列表卸载

安装后也可以打开“设置 → 应用 → 已安装的应用”，找到 `PaperMate 论文助手` 后点击卸载。
