node_modules/
.next/
data/
coverage/
*.tsbuildinfo
*.lnk
.env*

!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.local

.DS_Store
Thumbs.db
.idea/
.vscode/

artifacts/
# 部署脚本（scripts/update-installed-*.ps1）会在项目内留整份备份；它不属于源码，
# 也不能参与类型检查（tsconfig 同时排除了它，否则备份里的旧代码会把构建搞挂）。
asset-backups/

# 本机数据与备份：完整 JSON 备份体积大且含个人论文内容，绝不入库。
papermate-backup-*.json
# 编辑器/工具（DSH 记忆库等）的本地状态目录
.mnemon/
.npm-cache/
