# PaperMate 功能日志

> 本文档记录 PaperMate 每个版本的功能明细与变更内容，是项目长期维护的核心文档之一。
> 英文变更记录见 [CHANGELOG.md](CHANGELOG.md)，项目介绍见 [README.zh-CN.md](README.zh-CN.md)。

PaperMate 是一个本地优先、AI 辅助的论文阅读工具：导入带文本层的 PDF 后，按原版页面逐页展示，支持透明文本层划选段落进行多轮问答，并可生成阅读笔记、论文脑图和写作思路分析。

---

## 版本总览

| 版本 | 构建日期 | 类型 | 摘要 |
| --- | --- | --- | --- |
| [v4.2](#v42-2026-08-16) | 2026-08-16 | 正式发布 | 修复跨片段划选丢字；新增预编译包独立安装器（解压双击即装，不需要 Node.js）与开发版一键部署（`一键升级.bat`）；预编译包卸载残留修复；安装入口合并为一个；清理失效代码与冗余文件 |
| [v4.1](#v41-2026-08-16) | 2026-08-16 | 正式发布 | 修复应用内更新无法替换程序文件的根因（新增安装启动器，助手在安装目录外运行）；失败不再卡死并释放更新锁；新增安装阶段进度、可恢复的更新窗口与连接告警；窄屏折叠与阅读器重绘优化 |
| [v4.0.1](#v401-2026-08-16) | 2026-08-16 | 补丁发布 | 修复提示词缓存只按修改时间判断导致读错文件内容的问题；CI 增加 Windows 矩阵并修正更新器测试的平台假设 |
| [v4.0](#v40-2026-08-16) | 2026-08-16 | 正式发布 | 新增联网搜索（智谱/博查/Tavily/自定义）、翻译与自动翻译、应用内自动更新（含校验与自动回滚）；界面美化、操作逻辑优化、修复多项 bug |
| [v3.6](#v36-2026-08-16) | 2026-08-16 | 正式发布 | 界面视觉刷新（visual-refresh.css）；论文库按最近阅读排序；陪读小人尺寸可调；提示词优化并新增同步脚本；修复 bug |
| [v3.3](#v33-2026-08-16) | 2026-08-16 | 正式发布 | 阅读器新增 Alt+左键拖动平移与双击选词；重写翻译提示词修复翻译输出问题 |
| [v3.2](#v32-2026-08-16) | 2026-08-16 | 正式发布 | 一键安装/更新改为增量更新（源码指纹判断，无变化跳过复制构建）；优化操作逻辑、修复 bug |
| [v3.1](#v31-2026-08-16) | 2026-08-16 | 正式发布 | 新增自定义模型（OpenAI 兼容，设置中自由增删改）；模型体系重构（内置 4 模型）；体验优化 |
| [v3.0.1](#v301-2026-08-16) | 2026-08-16 | 补丁发布 | 提示词快照入库、陪读小人人格与界面微调、README 界面展示更新（问答/陪读小人截图）、截图同步 |
| [v3.0](#v30-2026-08-16) | 2026-08-16 | 正式发布 | 新增 AI 陪读小人（5 人格）、20 套阅读主题、GLM-4-Flash 双档模型；优化提示词、美化界面、修复 bug |
| [v2.9](#v29-2026-08-16) | 2026-08-16 | 正式发布 | Kimi 升级 kimi-k2.6；阅读笔记/论文脑图/写作思路支持历史版本查看、修改与删除，生成失败不覆盖原数据；优化提示词与体验 |
| [v2.8](#v28-2026-08-16) | 2026-08-16 | 正式发布 | 新增 Kimi 模型、拾句库与提示词库（public/ 纯文本可编辑）、API Key 本地存储与快捷增删改查；修复侧边栏 bug |
| [v2.1](#v21-2026-08-16) | 2026-08-16 | 正式发布 | 设置面板整合重构（API Key、阅读主题、备份统一为单一设置面板）；README 详细介绍当前功能；截图合并为单张设置图 |
| [v2.0](#v20-2026-08-16) | 2026-08-16 | 正式发布 | 引用图表链接跳转、注释删除、论文库拖动置顶、主题配色完善；优化模型调用/提示词/存储/性能；一键安装即装即更 |
| [v1.1](#v11-2026-08-16) | 2026-08-16 | 正式发布 | 新增免费智谱 GLM 接口、主页论文元数据解析与一键升级；存储重写为 SQLite；优化问答逻辑与提示词 |
| [v1.0](#v10-2026-08-16) | 2026-08-16 | 首版发布 | 本地优先的 AI 辅助论文阅读工具：原版页面阅读、段落划选问答、笔记/脑图/写作思路生成、Windows 一键安装 |

> 维护约定：每次发布新版本时，在表格顶部（最新在上）新增一行，并在下方按模板追加对应章节。

---

## v4.2（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **预编译包独立安装器**：Release 里的 `papermate-windows-x64.zip` 此前只能被应用内更新助手消费，新用户把 zip 下载下来之后无从安装。现在包内自带 `一键安装.bat` 与 `scripts/install-package.ps1`：解压后双击、选择安装位置，几十秒装完，不需要 Node.js / npm / 联网（包内自带 `node.exe`）。覆盖安装会先把整个旧目录挪成同级 `.papermate-backup-<guid>`（想回滚就先停服务再把备份目录改回原路径），再还原 `data\` 与用户改过的 `public\*.txt`——与包内默认值逐字节比对，只有确实改过才还原，没改过的仍用新版默认值。也可以把 zip 直接拖到 `一键安装.bat` 上安装。安装完成后创建桌面/开始菜单快捷方式并注册到 Windows 应用列表；包内同时带 `一键卸载.bat`，安装目录自己就能卸载，不必依赖开始菜单。
- **开发版一键部署**（`一键升级.bat` → `scripts\upgrade-local.ps1`，引擎 `scripts\quick-patch.ps1`）：完整发版链路要打包 8000+ 个文件、再压缩、再校验 SHA-256、再让更新助手换目录，验证一个改动太慢。现在双击一个 bat 就够了：脚本先判断本机安装形态——**预编译包安装**走"只覆盖 `.next`（约 8 MB）、`server.js`、`package.json`、5 个运行时脚本（apply-update、launch-update、start-papermate、stop-papermate、uninstall）"，**源码安装**走 `install.ps1 -Upgrade` 增量升级；预编译包这条会先 dry-run 校验并按需应用 `.patch`，跑测试、构建，把安装目录当前的运行时备份到 `%LOCALAPPDATA%\PaperMate\dev-backups\<时间戳>`（默认保留最近 5 份），覆盖后重启服务、回读上报版本号并打开页面。`-Undo` 回滚上一次，`-ListBackups` 列出可回滚的备份。依赖清单有变化时会自动补拷 standalone 追踪到的 `node_modules`，避免覆盖完启动即 MODULE_NOT_FOUND。
- **`一键升级.bat` 重新加入（这次职责明确）**：v4.0 曾把它连同 `scripts/upgrade.ps1` 当作冗余移除，但安装说明一直在引用它，实际也少了"把当前源码推到已安装版本"的入口。现在它按安装形态分派：**预编译包安装**走开发版部署（构建后只覆盖运行时产物），**源码安装**走 `install.ps1 -Upgrade` 增量升级。
- **安装入口合并为一个**：原来的 `安装PaperMate.bat` 和 `一键安装.bat` 在"安装"这件事上重复，而且前者放在源码目录里根本跑不了——源码目录没有 `server.js` / `node.exe`，包体校验会直接报错。现在只保留 `一键安装.bat`：`scripts/install-local.ps1` 先看当前目录是预编译包还是完整源码，再分别交给 `install-package.ps1` 或 `install.ps1`（已装过同一份源码时自动转增量升级）。发布包里也换成 `一键安装.bat`，并补上 `一键卸载.bat`（以前预编译包装完，安装目录里找不到卸载入口）。仓库根目录现在正好三个入口：`一键安装.bat` / `一键升级.bat` / `一键卸载.bat`。

### 问题修复

- **划选丢片段（划选结果不完整）**：旧实现在一行横跨多个文本片段时，只在两个指针所在片段之间取一个列带（当两个矩形重叠宽度超过较窄矩形宽度的 35% 时，直接把交集当成列带），并且只收录中心点落在该列带内（±2px）的片段。而 PDF 文本层里一个视觉行常被拆成几十个绝对定位的 span，行尾的上标、行内特殊符号（如 `⋆`）与最后一个词的中心点都不落在这个窄带里，于是被整段丢掉——实测中间行分别丢掉 10/23 与 15/41 个片段（`extreme`、`biased`、`Enrico`、`Zio`、上标 `c`/`d`/`e` 等）。现在把范围计算抽成纯函数 `lib/selection-range.ts`：先按"垂直重叠超过较矮矩形的一半"把片段合并成视觉行，再按间隙（`max(6, 行高中位数 × 0.8)`）把行切成段；锚点所在的段整段参与、与锚点列带（±6px）相交的段也整段参与，锚点所在的行始终参与，最后按首末片段的索引窗口裁剪，`components/pdf-reader.tsx` 只负责把 span 交给它并拼装引文。修复后逐片段核对：中间三行 23 / 41 / 29 个片段全部覆盖，未覆盖 0（修复前分别是 10 / 15 个）。
- **预编译包安装卸载不干净**：`uninstall.ps1` 只把 `.papermate-installed.json` 当作"这是 PaperMate 安装目录"的凭证，而预编译包安装里没有这个文件，于是卸载时只清掉快捷方式、启动器和 Windows 应用列表项，程序文件（约 260 MB）被留在原地。现在同时接受预编译包标记（`papermate-version.json` + `server.js` + `node.exe`），预编译包安装也能整目录卸干净，`data\` 依然默认保留（`-RemoveAllData` 才连数据删）。受保护路径判断也必须排除这种情况——预编译包安装的 `sourceProjectDir` 等于安装目录自身。
- **源码升级器不再静默覆盖预编译包安装**：对"有 `server.js` / `node.exe`、但没有源码标记"的目录执行 `install.ps1 -Upgrade` 时，旧行为会直接拿源码覆盖，既破坏包内运行时结构也可能把版本降回去。现在明确拒绝并给出三种可选做法：应用设置里的「检查更新」、下载 Release 的预编译包、或用「一键安装.bat」选一个新的安装位置装源码。

### 功能调整

- **引用语料扩充**：`public/quotes.txt` 新增 264 条。
- **安装文档重写**：`INSTALL.md` / `安装说明.md` 与两个 README 围绕新流程重写——预编译包（推荐，不需要 Node.js）与源码安装两条路径、统一的 `一键安装.bat` 入口、拖拽 zip 安装、开发版部署；开发版版本号固定为 1 的约定也写进文档。
- **仓库精简**：移除已失效的 `scripts/update-installed-*.ps1` 部署脚本（写死本机安装路径，且与 `一键升级.bat` 重复）、过期的提示词快照 `public/prompts0818.txt`、两张没有任何文档引用的界面截图；`截图/` 下的示例产物同步刷新。

### 工程与质量

- 划选逻辑从 `components/pdf-reader.tsx` 抽成纯函数模块（`selectItemIndexes` / `buildSelectionQuote`），新增 12 条回归测试：跨片段列带、指针落在行顶边、双栏隔离、索引窗口限定、首末偏移裁剪、blockIds；整库 **17 个文件 232 条测试**通过。
- 打包脚本的断言覆盖到面向用户的入口：`一键安装.bat`、`一键卸载.bat`、`scripts/install-package.ps1` 必须进包，`scripts/quick-patch.ps1` 必须不进包（开发工具，预编译安装里没有源码）；`release.yml` 解压后会校验同一份清单。
- 清理失效代码：把 45 个"定义了但全仓库没人 import"的导出降级成模块内部符号，删掉 2 个完全没人用的声明（`lib/db.ts` 的 `writeDiskBackup`、`lib/pdf.ts` 的 `TextLineCluster`）。清理后 `tsc --noEmit`、232 条测试与生产构建全部通过。

### 已知限制 / 注意事项

- `一键升级.bat` 会先看本机安装形态：预编译包安装走"构建当前源码并覆盖运行时产物"，源码安装走 `install.ps1 -Upgrade`。但预编译包那条要求当前目录是一份完整的开发版源码；如果手上只有从 Release 下载解压出来的包（没有源码），请改用应用内「检查更新」或重新下载 Release 的预编译包。
- 开发版部署会把安装目录的 `package.json` 版本改成开发版的 1，并在 `papermate-version.json` 标记 `devBuild: true`；因此应用内更新会一直提示「有新版本 4.x」，点它会把这台机器恢复成正式包。正式版本号只在纯净上传仓库里维护。
- 预编译包安装器目前只支持 Windows x64。

---

## v4.1（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **安装阶段进度**：更新助手在执行过程中按里程碑回写 `installStage` / `installProgress`（校验安装包 5% → 解压程序文件 10–35% → 停止旧服务 40% → 备份旧版本 50% → 保留论文与设置 60% → 替换程序文件 75% → 启动新版本 85% → 验证启动状态 90% → 完成 100%），回滚时也会写入状态。界面因此有了独立于下载进度的**安装进度条**与阶段文字（`scripts/apply-update.ps1`、`lib/update-types.ts`、`components/update-manager.tsx`）。
- **可恢复的更新窗口**：关闭更新弹窗（按钮或 `Esc`）不再"丢失"更新，右下角保留「更新进行中 · 查看进度」入口；设置页的按钮在更新进行中变为「查看更新进度」。
- **安装期间连接告警**：安装状态超过 2 分钟没有推进、或服务重启期间超过 90 秒无法连接时，给出明确提示（可关闭窗口、请勿重复启动安装），避免误判为卡死；更新完成后出现「刷新页面」按钮。

### 问题修复

- **应用内更新无法替换程序文件（根因修复）**：旧实现直接以安装目录为工作目录启动更新助手。Windows 在进程仍占用该目录时会拒绝替换其中的文件，导致更新失败。现改为把启动器 `scripts/launch-update.ps1` 写入 updates 目录（**安装目录之外**）并从那里启动；启动器再以 `-WorkingDirectory`（updates 目录）拉起真正的助手，把输出重定向到 `install.log` / `install-error.log`，并通过 `helper-pid.json` 把助手进程号交回应用（`lib/updater.ts`、`scripts/apply-update.ps1`、新增 `scripts/launch-update.ps1`）。
- **更新失败不再卡死**：此前助手退出无人观察，应用可能永远停在 `installing`，且更新锁不释放——更新失败后用户连提问都做不了。现在启动器非 0 退出会报明确错误（含退出码与诊断日志路径），启动器正常退出但未交接 PID 也会报错，所有失败路径统一 `unlockUpdate()` 并清除锁文件。
- **窄屏折叠不释放空间**：折叠原先只改透明度，左栏/助手面板折叠后仍占位。现折叠项从栅格中移除并释放列宽。
- **阅读器反复重绘 PDF**：侧栏开关或拖拽宽度时会连续触发 PDF 重绘。现三列宽度直接以最终值初始化，阅读器等布局稳定（120ms 防抖 + requestAnimationFrame）后再提交新宽度（`components/pdf-reader.tsx`、`app/globals.css`）。

### 工程与质量

- 更新器测试新增 5 条用例（助手失败恢复、从安装目录外启动并写日志、捕获助手非正常退出、终态错误释放更新锁、启动器交接 PID），总数 28 → 33。
- 发布包的必需文件断言加入 `scripts/launch-update.ps1`——它是更新器真正 spawn 的文件，缺失会导致安装阶段直接失败，因此纳入 `scripts/package-update.mjs` 与 `.github/workflows/release.yml` 的打包体检。

---

## v4.0.1（2026-08-16）

**类型**：补丁发布 · **构建日期**：2026-08-16

### 问题修复

- **提示词缓存读错文件**：`public/prompts.txt`（任务/系统/联网三块提示词）与 `public/buddy-personas.txt` 的缓存原本只按「文件修改时间」判断，**没有带文件路径**。同一毫秒内写入的两个不同文件 mtime 相同，第二次读取会错误命中第一次的缓存，返回上一个文件的内容。现缓存 key 增加解析后的文件路径（`lib/prompts.ts` 的 `loadPrompts` 与 `loadBuddy` 两处）。该问题在 Linux 上稳定复现（连续写文件天然同毫秒），Windows 上偶发；回归测试已改为把两个文件的时间戳显式设为同一个值，未修复时在任何平台都会失败。
- **CI 在 Linux 上失败**：应用内自动更新是 Windows x64 专属功能（`lib/updater.ts` 的 `supported` 判定），但更新器测试跑在 `ubuntu-latest` 上并按 Windows 行为断言，导致 22 条用例失败。现这些用例在非 Windows 平台跳过而非失败，同时 CI 改为 `ubuntu-latest` + `windows-latest` 双系统矩阵，更新器测试在 Windows 上仍完整运行。
- **更新器测试偶发失败（用例串扰）**：「重复点击不会重复下载」用例发起后台下载后没有等待其结束。由于状态文件路径在写入时才按 `PAPERMATE_APP_DATA` 解析，这次陈旧的下载会跨到后续用例继续运行、读到后续用例的 `fetch` 桩，并把自己的消息写进后续用例的 `status.json`——于是「声明体积与实际不符」用例偶发读到「更新包校验失败」的消息。现所有真正发起下载的用例都会等待终态（ready/error）后才结束。

### 工程与质量

- `.github/workflows/ci.yml` 增加操作系统矩阵；`lib/updater.test.ts` 增加平台跳过说明。

---

## v4.0（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **联网搜索**：新增三态联网开关——关闭、自动（仅当问题涉及外部事实、时效性、工具/产品/价格/版本时才检索）、强制（每次提问都检索）；检索结果以编号来源返回，正文生成可点击的编号角标，消息下方展示「联网来源」列表。搜索是与聊天模型解耦的独立服务，DeepSeek、GLM、Kimi 与自定义模型都能联网；只把提问原文发给搜索服务商，论文正文不离开本机；任何失败都降级为「不联网回答」，不阻断提问。服务商支持智谱（`search_std`/`search_pro`/`search_pro_sogou`/`search_pro_quark` 四档引擎）、博查、Tavily 与自定义接口（`lib/web-search.ts`、`lib/web-search-store.ts`、`app/api/web-search/route.ts`、`data/search.json`）。
- **翻译与自动翻译**：`Alt`+`T` 翻译当前选区；`Alt`+`Shift`+`T` 切换「自动翻译」，开启后每次新划选自动翻译（每次都是新的单选段会话，不与上一次选区合并）。选单句或整段时译文后附带简短语法讲解，多片段选区分段标注 `# 1`/`# 2`。
- **应用内自动更新**：后台检测 GitHub 正式 Release，仅在有新版本时弹窗并展示更新说明；下载预构建 Windows 包（自带 Node 运行时）并做 SHA-256 校验；独立助手负责停服务、备份旧安装、替换程序文件、健康检查，失败**自动回滚**。检测**不调用 api.github.com**，改走 `github.com/releases/latest` 跳转与 Atom 订阅源，避免未认证 60 次/时限流导致无法更新（`lib/updater.ts`、`lib/update-version.ts`、`lib/update-guard.ts`、`lib/update-activity.ts`、`components/update-manager.tsx`、`app/api/updates/route.ts`、`scripts/apply-update.ps1`、`scripts/package-update.mjs`）。
- **联网提示词可自定义**：`public/prompts.txt` 新增 `[websearch]` 块，可编辑引用规则。
- **构建期提示词同步**：新增 `scripts/sync-prompt-defaults.mjs` 覆盖任务提示词与联网提示词。

### 功能调整

- **界面美化**：统一主题强调色与陪读小人配色，优化排版；删除成果及其历史版本的确认框由浏览器原生 `confirm` 改为主题化应用内弹窗（红色删除按钮，支持 Esc/点击遮罩取消）。
- **操作逻辑优化**：更新接口拒绝跨站页面请求但不拦截应用自身请求（同源判断基于原始 `Host` 头，因为浏览器对同源 POST 不一定发送 `Origin`）；安装更新前等待进行中的翻译、提问与保存结束，替换期间暂停新的模型请求。
- **聊天输入**：`Enter` 直接发送、`Shift`+`Enter` 换行，兼容中文输入法选词回车不误发送。
- **陪读小人人格放宽**：可主动提问、反问、抛话题；新增「导师」人格；对话内容改为携带原文选段+提问+回答或论文摘要，闲聊频率随话痨滑块变化。

### 问题修复

- 阅读笔记/论文脑图/写作思路生成失败不再覆盖原有数据；失败改为右下角可复制、可关闭的警示浮层，有旧成果时恢复旧内容。
- 对话发送失败不再写入对话记录（复用会话恢复原状态，新建空会话整体移除），未发送内容写回输入框。
- 移除不存在的「DeepSeek Max」档位，模型标签与真实代号对齐（`glm-4-flash`、`glm-4.7-flash`、`deepseek-v4-flash`、`kimi-k2.6`）；快速/深度按钮不再切换模型档位，改为当前所选模型的「非思考/思考」开关。
- 陪读小人等级外观修复（笔记本只属于 L5 台式电脑档、手稿/放大镜/咖啡杯/权杖/文献小山均贴地不悬浮）；气泡宽度放宽至 360px 并按位置自动翻转方向，不再被屏幕边缘裁切。
- `Ctrl`/`Cmd`+滚轮缩放只作用于页面本体，页码标签恒定大小；网格槽高精确补偿，缩放提交不再竖向抖动；光标位于工具栏、页码、间隙或留白时也能解析锚点；高缩放按像素预算降低有效 DPR，位图内存有界。
- Kimi 模型名由 `kimi-k2.5` 修正为实际可用的 `kimi-k2.6`（按 `kimi-k3` → `kimi-k2.5` 回退），修复「验证连接通过但实际生成 404」。
- 论文题目/关键词/期刊解析结合首页版面块、Crossref 与 OpenAlex 查询及本地抽取，合并折行标题并排除期刊页眉。
- 按新的 `papermate.png` 重新生成图标，一键安装创建的快捷方式同步使用新图标。
- 移除过时的 `scripts/upgrade.ps1` 与 `一键升级.bat`，升级统一由 `一键安装.bat` 处理。
- 移除已废弃的 `/api/deepseek/*` 路由。

### 工程与质量

- 测试扩展到 16 个文件、215 条用例，覆盖会话、论文库排序、联网搜索、提示词加载、更新器、更新同源校验、版本比较、模型配置、存储与 PDF 处理。
- 新增 `.github/workflows/release.yml`：按 `vX.Y.Z` 标签构建、校验（密钥泄漏断言 + 真实启动冒烟测试）并上传草稿 Release。

---

## v3.6（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **界面视觉刷新**：新增共享样式 `app/visual-refresh.css`（由 `app/layout.tsx` 引入），统一主题强调色与陪读小人配色，各主题排版与 PDF 坐标规则仍保留在 `globals.css`。
- **论文库按最近阅读排序**：论文列表按「置顶 → 最近阅读时间 → 创建时间」排序（`lib/library-order.ts`）；阅读时间戳贯穿存储层与论文接口（`lib/types.ts`、`lib/storage.ts`、`lib/db.ts`、`/api/storage/papers/[id]`）。
- **陪读小人尺寸调节**：新增缩小/放大按钮，可在 40%–180% 之间调整陪读小人大小，选择保存在本机（`lib/buddy-layout.ts`、`components/buddy-system.tsx`）。
- **提示词同步脚本**：新增 `scripts/sync-prompt-defaults.mjs`，保持可编辑的 `public/prompts.txt` 与内置默认提示词一致。

### 功能调整

- **提示词优化**：各任务提示词与陪读小人人格提示词优化（`lib/prompts.ts`、`public/prompts.txt`）。
- **应用图标更新**：`papermate.ico`、`papermate-uninstall.ico`、`papermate.png` 重新生成。

### 问题修复

- 修复多项 bug，并清理部分代码。

### 工程与质量

- 新增测试：陪读小人布局（`lib/buddy-layout.test.ts`）。

---

## v3.3（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **Alt + 左键拖动平移**：按住 `Alt` 时在阅读器内左键拖动即可自由平移论文内容（松开恢复），配合工具栏「小手」拖动模式使用；移除了原来的「空格+左键」临时平移（空格会触发页面下滚，体验不佳）（`components/pdf-reader.tsx`、`app/globals.css`）。
- **双击选词**：在原文文本层双击任意单词（含 self-attention、p.3、BLEU-4 等带连字符/点号的术语与编号），立即选中该词并高亮，可在右侧直接提问；翻译模式开启时双击选词会自动翻译（`components/pdf-reader.tsx`）。

### 问题修复

- **翻译输出问题**：重写 `[translate]` 翻译提示词——精简为直接输出译文、禁止开场白/结束语/总结等无关内容、多片段按 `# 1`/`# 2` 标注并按顺序输出、单句或段落译文后追加语法解释教学；同步更新 `public/prompts.txt`、内置默认提示词（`lib/prompts.ts`）与旧版 DeepSeek 路由（`app/api/deepseek/chat/route.ts`）。

---

## v3.2（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 功能调整

- **一键安装增量更新**：安装器计算参与构建的源码指纹（路径 + 大小 + 修改时间，SHA256，排除 node_modules/.next/.git/data/截图/scripts 等不影响构建的内容）；源码无变化时跳过复制与构建，有变化时 robocopy 只复制变更文件，更新速度显著提升（`scripts/install.ps1`）。
- **操作逻辑优化**：界面与交互操作逻辑改进（`app/page.tsx`、`components/pdf-reader.tsx`、`app/globals.css`）。

### 问题修复

- 修复多项 bug。

---

## v3.1（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **自定义模型**：设置中自由增删改 OpenAI 兼容的 chat/completions 模型（自定义地址、模型名与 API Key），`/api/chat` 按保存的配置请求（`lib/models.ts`、`lib/models-store.ts`、`/api/storage/models`）。

### 功能调整

- **模型体系重构**：内置 4 个模型（GLM-4-Flash、GLM-4.7-Flash、DeepSeek、Kimi），展示与请求参数集中管理；快速/深度按钮只切换"思考"开关，不再换模型。
- **体验优化**：模型选择与设置交互优化（`app/page.tsx`、`app/globals.css`）。

### 工程与质量

- 新增测试：模型定义与校验（`lib/models.test.ts`）。

---

## v3.0.1（2026-08-16）

**类型**：补丁发布 · **构建日期**：2026-08-16

### 新增功能

- `public/prompts0818.txt` 提示词快照纳入仓库。

### 功能调整

- 陪读小人人格提示词与界面样式微调（`components/buddy-system.tsx`、`public/buddy-personas.txt`、`app/globals.css`）。
- README 界面一览更新：新增问答与陪读小人截图，样例文件与截图文件夹同步至当前项目。

---

## v3.0（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **AI 陪读小人**：五种人格（毒舌、温柔、哲思、鼓励、导师），随提问类型与回答完成等场景自动回应；可一键显示/隐藏，隐藏后完全卸载、不再请求模型；人格提示词在 `public/buddy-personas.txt` 中可编辑（按文件修改时间缓存，编辑后下次请求生效），无 API Key 或模型调用失败时使用本地兜底语料（`components/buddy-system.tsx`、`app/api/buddy`）。
- **20 套阅读主题**：在原有 10 套基础上新增 10 套（机密绝密档案、电子墨水拟真等）。
- **GLM 双档模型**：新增 `glm-4-flash`（智谱免费模型，官方支持高并发）作为 GLM 主模型，`glm-4.7-flash` 互为备用并自动回退。

### 功能调整

- **提示词优化**：各任务提示词进一步优化（`lib/prompts.ts`、`public/prompts.txt`）。
- **界面美化**：整体视觉与交互细节美化（`app/globals.css`、`app/page.tsx`）。

### 问题修复

- 修复多项 bug。

---

## v2.9（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **成果历史版本管理**：阅读笔记、论文脑图、写作思路三类成果保留历史版本，可查看之前生成的数据、修改或删除；生成失败不会覆盖原有数据（每类成果保留数量有限，超出时丢弃最旧版本）。

### 功能调整

- **Kimi 升级**：模型由 `kimi-k2.5` 升级为 `kimi-k2.6`，并支持备用模型列表（模型可用性随账号变化，实测旧模型已 404）。
- **提示词优化**：各任务提示词进一步优化（`lib/prompts.ts`、`public/prompts.txt`）。
- **应用图标更新**：`papermate.ico`、`papermate.png` 重新生成。

### 问题修复

- 体验优化与多项 bug 修复。

### 工程与质量

- README 界面一览新增提示词库截图。

---

## v2.8（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **Kimi 模型**：接入 Moonshot Kimi（`kimi-k2.5`）作为第三个模型提供方，设置面板独立 API Key 与连接验证；DeepSeek 与 Kimi 支持并发对话，GLM 免费档保持单任务（`app/api/chat`）。
- **拾句库**：侧边栏展示拾句并支持刷新换句；内容保存在 `public/quotes.txt`（纯文本，每行一句，`#` 开头为注释），可直接增删改查（`lib/quotes.ts`）。
- **提示词库**：翻译、上下文解释、概念讲解、自由问答、阅读笔记、论文脑图、写作思路七个任务的提示词保存在 `public/prompts.txt`（每个任务以独占一行的 `[任务名]` 开始），直接编辑即可调整，保存后下次提问自动生效；删除某个任务块会回退到代码内置的默认提示词（`lib/prompts.ts`）。
- **API Key 本地管理**：密钥改存本机 `data/apikey.txt`（明文，`data/` 不提交 Git，浏览器不持有密钥），设置面板可快捷增删改查（`lib/api-keys.ts`、`/api/storage/apikey`）。

### 问题修复

- 修复侧边栏相关 bug。
- 其他体验与稳定性修复。

### 工程与质量

- 新增测试：拾句库（`lib/quotes.test.ts`）、提示词（`lib/prompts.test.ts`）。

---

## v2.1（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 功能调整

- **统一设置面板**：原"模型设置/API 设置"与阅读主题、备份管理整合为单一"设置"面板——双栏布局（主栏：模型提供方与 API Key 验证；侧栏：阅读主题与备份管理），带标题头部与"完成"按钮；页面头部按钮统一更名为"设置"。
- **设置面板样式重构**：面板加宽、分区化布局（`settings-block`）、稳定的关闭按钮与双栏网格（`app/globals.css`、`app/page.tsx`）。
- **README 正式版介绍**：中英文 README 重写为详细的功能说明（论文库、阅读与划选、阅读主题、AI 助手、生成成果、存储与备份、Windows 一键安装/更新/卸载、环境要求、使用方式、界面一览、数据与隐私、已知限制），界面一览改用合并后的单张设置截图 `截图/设置.png`。

### 工程与质量

- 截图整理：原 `api设置1.png` / `api设置2.png` 合并为单张 `设置.png`，README 引用同步更新。

---

## v2.0（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **论文引用图表链接跳转**：阅读成果中的引用、图表链接可点击，直接跳转到论文对应页或打开外部 URL（仅限原 PDF 自带链接区域，不做文本匹配）。
- **注释删除功能**：可删除已保存的对话等注释内容。
- **本地论文库拖动置顶**：论文库支持拖拽排序，并可置顶常用论文（`/api/storage/papers/order`）。
- **主题配色完善**：统一 CSS 变量调色板，界面配色更协调（`app/globals.css`）。

### 功能调整

- **模型调用逻辑优化**：DeepSeek 与智谱 GLM 双提供方的流式调用、上下文截断与错误处理优化（`app/api/chat`）。
- **提示词优化**：翻译、上下文解释、概念讲解、自由问答、阅读笔记、论文脑图、写作思路七个任务的提示词进一步完善（`lib/prompts.ts`）。
- **持久化存储优化**：SQLite 写入路径与工作区数据管理优化（`lib/storage.ts`、`lib/db.ts`）。
- **安装流程简化**：移除独立的一键升级脚本（`一键升级.bat`、`scripts/upgrade.ps1`）；`一键安装.bat` 自动检测——有旧版本直接覆盖更新并保留 `data`，无旧版本则全新安装。
- **性能优化**：缩放渲染与位图内存等性能改进。

### 问题修复

- Ctrl/Cmd+滚轮缩放系列问题：页码标签保持恒定、页面提交不抖动、锚点解析稳定、小步/快速来回缩放不再反复全量重渲染、高缩放下单页 canvas 内存有界（详见 CHANGELOG）。

### 工程与质量

- 文档随安装流程调整同步更新（README、安装说明、INSTALL）。

---

## v1.1（2026-08-16）

**类型**：正式发布 · **构建日期**：2026-08-16

### 新增功能

- **免费智谱 GLM 接口**：接入智谱开放平台免费模型 `glm-4.7-flash`，与 DeepSeek 双提供方可选，统一走 `/api/chat` 流式接口。
- **主页论文元数据解析**：导入论文时自动解析标题、关键词、期刊与影响因子——综合首页版面块、Crossref / OpenAlex 在线查询与本地关键词、期刊规则；自动合并折行标题、排除期刊页眉（`lib/paper-metadata.ts`）。
- **一键升级**：新增 `一键升级.bat` 与 `scripts/upgrade.ps1`，直接用当前项目源码升级已安装版本并保留 `data` 数据；`一键安装.bat` 检测到已安装版本时自动进入升级模式。
- 安装器版本元数据改为从 `package.json` 动态读取，不再硬编码。

### 功能调整

- **数据持久化重写**：主存储由浏览器 IndexedDB 改为本机 SQLite 数据库 `data/papermate.db`（Node.js 内置 `node:sqlite`，要求 Node.js ≥ 22.5）；PDF、高亮、对话、笔记与生成内容全部入库。
- **备份机制调整**：完整 JSON 备份改为手动触发（立即备份、从磁盘恢复、导出、导入）；旧版 `papermate-backup.json` 与旧 IndexedDB 数据在首次启动时自动迁移一次，迁移后作为历史数据保留。
- **问答逻辑优化**：上下文解释先回答输入框中的问题，再结合论文全文与选段展开；回答区分“原文明确表述”与“基于论文证据的推理”，证据标注页码。
- **提示词全面优化**：翻译、上下文解释、概念讲解、自由问答、阅读笔记、论文脑图、写作思路七个任务均重构为结构化提示词（`lib/prompts.ts`），统一约束输出格式、证据标注与术语一致性。
- **模型接口重构**：`/api/deepseek/*` 合并为 `/api/chat`（多提供方），密钥验证独立为 `/api/test-key`。

### 问题修复

- 修复上一版遗留的若干问题（以存储迁移、元数据解析与问答输出为主）。

### 工程与质量

- 新增测试：存储层（`lib/storage.test.ts`）、论文元数据（`lib/paper-metadata.test.ts`）、提示词（`lib/prompts.test.ts`）。

### 已知限制 / 注意事项

- 仍仅支持带文本层的可检索 PDF，不包含 OCR，不支持 DOCX。
- 数据保存在安装目录 `data/papermate.db`；卸载时默认保留 `data` 目录。
- 运行要求 Node.js ≥ 22.5（依赖内置 `node:sqlite`）。
- 无账号体系与云端同步，数据仅存于本机。

---

## v1.0（2026-08-16）

**类型**：首版发布 · **构建日期**：2026-08-16

### 核心功能

- **原版页面阅读**：使用 PDF.js 按论文原版排版渲染页面，透明文本层保证划选位置与原文一致。
- **段落划选提问**：划选一段原文即可提问，支持 `Ctrl`/`Cmd` 追加片段，一次最多组合 20 个片段。
- **多轮对话**：可针对当前选区追问，也可自由提问；提问时自动附带当前选区作为上下文。
- **两种模型模式**：`Flash` 适合翻译和普通问答；`MAX 思考` 适合解释、总结和写作分析。
- **快捷指令**：内置翻译、结合上下文解释、概念讲解、详细讲解等常用指令。
- **结构化阅读成果**：
  - 阅读笔记：Markdown 文本，关键结论标注原文页码与证据，支持数学公式渲染。
  - 论文脑图：可折叠的论文论证结构脑图，导出为 `.svg` 图片。
  - 写作思路：写作策略分析，包含可迁移的段落与句式框架（Markdown）。

### 论文库与检索

- 拖入 PDF 或点击选择文件导入论文，支持搜索论文。
- 论文按来源哈希去重管理（`lib/source-hash.ts`）。

### 数据与存储

- **本地优先存储**：PDF、高亮、对话、笔记和生成内容保存在浏览器 IndexedDB（`idb`）。
- **自动磁盘镜像**：数据自动镜像到项目目录 `data/papermate-backup.json`，清空浏览器缓存后仍可恢复。
- **整库备份管理**（设置面板）：立即备份、从磁盘恢复、导出 JSON 备份文件、在其它电脑上导入备份。
- **结果文件导出**：阅读笔记与写作思路按“论文标题-成果类型”命名下载为 `.md` 文件，脑图下载为 `.svg` 文件。

### 模型接入

- 接入 DeepSeek Chat Completions 流式接口（`/api/deepseek/chat`），逐字流式输出。
- 设置面板支持输入并验证 DeepSeek API Key（`/api/deepseek/test`）。
- API Key 只保存在当前页面内存，不写入 IndexedDB、备份文件、导出文件或服务器日志。

### Windows 应用化

- 一键安装脚本（`一键安装.bat`）：自动安装依赖、构建正式版本、创建桌面与开始菜单快捷方式、注册 Windows 已安装应用列表入口。
- 配套启动、停止、卸载脚本（`scripts/`），支持端口占用自动切换；卸载保留 `data` 数据目录。
- 专属应用图标与卸载图标（`papermate.ico`、`papermate-uninstall.ico`）。

### 工程与质量

- 技术栈：Next.js 15（App Router）、React 19、TypeScript、PDF.js、IndexedDB、react-markdown + KaTeX、ESLint、Vitest。
- 单元测试：PDF 解析（`lib/pdf.test.ts`）、脑图生成（`lib/mindmap.test.ts`）、备份（`lib/backup.test.ts`）。
- CI 流水线（`.github/workflows/ci.yml`）：lint、test、build 自动检查。
- 社区规范：Issue / PR 模板、Dependabot、CONTRIBUTING、CODE_OF_CONDUCT、SECURITY。

### 已知限制（首版）

- 仅支持带文本层的可检索 PDF，不包含 OCR 功能。
- 不支持 DOCX 等其他文档格式。
- 无账号系统与云端同步，数据仅存于本机。
- 模型请求仅支持 DeepSeek 提供方。

---

## 维护规范

### 版本号规则

- **主版本**（v1、v2…）：功能架构或数据格式发生重大变化时递增。
- **次版本**（v1.1、v1.2…）：新增或调整功能时递增。
- **补丁**（v1.0.1…）：缺陷修复、文档与构建调整时递增。

### 每次发版需同步更新

1. 本文档：更新“版本总览”表格，按模板追加新版本章节。
2. `CHANGELOG.md`：以英文追加该版本变更条目。
3. `package.json` / `package-lock.json`：同步版本号。
4. `README.zh-CN.md` / `README.md`：如有功能增减，更新功能列表与界面截图。
5. 如生成结果或备份格式有变，更新 `lib/types.ts` 及相关迁移说明。

### 更新模板

复制以下模板到“版本总览”表格下方，替换占位内容：

```markdown
## vX.Y（YYYY-MM-DD）— 版本标题

### 新增功能
- 

### 功能调整
- 

### 问题修复
- 

### 已知限制 / 注意事项
- 
```

---

## 未来规划（待定）

以下方向来自首版声明的边界，供后续版本规划参考（未承诺具体排期）：

- OCR 支持，使扫描版 PDF 也可阅读与划选。
- 更多文档格式（DOCX 等）与导出格式。
- 更多模型提供方接入与模型选择扩展（已支持 DeepSeek 与免费智谱 GLM）。
- 账号体系与云端同步。
